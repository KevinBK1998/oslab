int main()
{
decl
    int code;
    str filename,default;
enddecl
begin
    default = "odd.xsm";
    write("FileName:");
    read(filename);
    if (filename=="") then
        filename=default;
    endif;
    code = exposcall ( "Exec" , filename);
    write("exec failed");
    write(code);
    return 0;
end
}
